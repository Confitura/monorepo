
server:
  port: 9090

twitter:
  api-key: KEY
  api-secret: SECRET
  callback: http://localhost:8080/login
spring:
  datasource:
    url: jdbc:mysql://${docker-machine.ip}:33061/confitura?useLegacyDatetimeCode=false&serverTimezone=Europe/Warsaw&useUnicode=yes&characterEncoding=UTF-8
    username: confitura
    password: confitura
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
docker-machine.ip: 192.168.99.100
